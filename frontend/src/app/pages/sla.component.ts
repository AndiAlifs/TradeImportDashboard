import { Component } from '@angular/core';

@Component({
  selector: 'app-sla',
  standalone: true,
  template: `<h2>SLA <small>(Refactoring in progress...)</small></h2>`
})
export class SlaComponent {}