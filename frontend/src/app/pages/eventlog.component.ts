import { Component } from '@angular/core';

@Component({
  selector: 'app-eventlog',
  standalone: true,
  template: `<h2>Event Log <small>(Refactoring in progress...)</small></h2>`
})
export class EventlogComponent {}