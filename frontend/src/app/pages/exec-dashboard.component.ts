import { Component } from '@angular/core';

@Component({
  selector: 'app-exec-dashboard',
  standalone: true,
  template: `<h2>Exec Dashboard <small>(Refactoring in progress...)</small></h2>`
})
export class ExecDashboardComponent {}